package dev.undine.presentation.preferences

import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier

/**
 * 계정 탭 — 작성자 신원 프로필과 저장소별 매핑.
 *
 * **아직 비어 있다.** 프로필 관리는 후속 티켓이 채운다. 전체 초기화가 프로필을 지우지 않는다는
 * 규칙은 이미 골격이 세워 뒀다 — 이 탭은 항목별 복원만 내준다.
 */
@Composable
fun AccountPreferencesContent(modifier: Modifier = Modifier) {
    PreferencesTabPlaceholder(modifier)
}
