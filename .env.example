# MCP 자격증명 템플릿.
# 이 파일을 .env 로 복사해도 자동 로드되지 않는다 — 셸 프로파일(~/.zshrc)에 export 한다.
# (훅과 permissions.deny 가 .env* 읽기를 차단하는 것이 의도된 동작이다.)

# context7 — 라이브러리 문서 라이브 조회 (JGit·Compose API 확인용)
export CONTEXT7_API_KEY=""
